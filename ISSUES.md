# Stuff that needs to be worked on

Tests:

[] - Must add tests for tenant invite, tenant invite acceptance, tenant user deletion, tenant user `role` switching

UI/UX:

[] - Add buttons in `sign in` and `register` forms so that you can switch between the two pages.
[] - Add a `<- Back` button that will take them to the homepage, or from wherever they were before on the website.